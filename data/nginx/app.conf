server {
  listen 80;
  server_name entend.io;

  location / {
    # return 301 https://$host$request_uri;
    # proxy_pass http://client:9000;
  }
}

# server {
#   listen 443 ssl;
#   server_name entend.io;  
# }